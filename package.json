{
  "name": "audio-synthesis-bridge",
  "version": "0.1.0",
  "description": "OSC-to-WebAudio bridge for real-time audio synthesis in browser and Node.js environments",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
